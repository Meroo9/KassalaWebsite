# توثيق حل مشكلة قاعدة البيانات ومزامنة المحتوى المركزي (Centralized Persistent Database & Cloud Sync)

## 1. شرح المشكلة بالتفصيل
كانت بوابة موقع جامعة كسلا تعتمد في تخزين البيانات والتعديلات المدخلة عبر لوحة التحكم الإدارية (`/portal`) حصراً على الذاكرة المحلية للمتصفح (`localStorage`) بواسطة [contentService.js](file:///d:/KassalaWebsite/KassalaWebsite/src/services/contentService.js).
أدى ذلك إلى بقاء أي تعديل يجريه المشرف (مثل تعديل الكليات، الخدمات، الأخبار، معرض الصور، أو الثيم) محبوساً داخل متصفح المشرف فقط، بينما يرى بقية الزوار والطلاب على الأجهزة الأخرى البيانات الافتراضية الثابتة فقط دون أن تنعكس التعديلات الحقيقية عبر الإنترنت.

## 2. سبب الحدوث
- عدم وجود واجهة برمجة خلفية (Server Route) ومخزن سحابي/مركزي موحد يربط عمليات الحفظ بالخادم.
- اقتصار دوال `saveData` و `loadData` على واجهة المتصفح `window.localStorage` دون إرسال البيانات للـ Backend.

## 3. خطوات الحل
1. **إنشاء مسار API مركزي لقاعدة البيانات (`/api/content`)**:
   - بناء المعالج في [src/app/api/content/route.js](file:///d:/KassalaWebsite/KassalaWebsite/src/app/api/content/route.js).
   - دعم التخزين السحابي عالي السرعة (Vercel KV / Upstash Redis REST) مباشرة عبر متغيرات البيئة دون حزم إضافية.
   - دعم التخزين المستمر عبر ملف محلي [src/data/liveData.json](file:///d:/KassalaWebsite/KassalaWebsite/src/data/liveData.json) مع ذاكرة خادم سريعة ومطابقة مع [fallbackData.json](file:///d:/KassalaWebsite/KassalaWebsite/src/data/fallbackData.json).
2. **تطوير خدمة المحتوى ([contentService.js](file:///d:/KassalaWebsite/KassalaWebsite/src/services/contentService.js))**:
   - إبقاء الاسترجاع الفوري من الذاكرة المحلية لتفادي وميض الواجهات (Optimistic Instant UI).
   - إرسال طلبات خلفية تلقائية فورية (Background POST) إلى `/api/content` عند أي عملية حفظ أو تعديل أو حذف.
   - تفعيل المزامنة التلقائية عند إقلاع الموقع على أجهزة الزوار (`syncWithServer`) لجلب أحدث البيانات من الخادم.
3. **تحديث صفحات العرض العامة لاستقبال التحديثات الحية**:
   - ربط صفحات الرئيسية والكليات والمعرض والتواصل بمستمعي الأحداث (`storage`) لتحديث الشاشات لحظياً عند ورود بيانات جديدة.
4. **تعزيز لوحة التحكم (`/portal`) بمؤشر المزامنة**:
   - إضافة شارة حالة اتصال قاعدة البيانات السحابية/الخادم وزر "مزامنة سحابية شاملة".
5. **التحقق والاختبار**:
   - نجاح البناء الإنتاجي `npm run build` لكافة المسارات الـ 16 بنسبة 100%.

## 4. الدروس المستفادة
- في تطبيقات الويب الحديثة، يجب دائماً فصل الـ Client Cache عن مصدر الحقيقة المركزي (Source of Truth)، واستخدام نمط التخزين الهجين (Optimistic UI + Server/Cloud Sync) لضمان السرعة الفائقة مع الاستمرارية الشاملة لكافة المستخدمين.
