# توثيق حل مشكلة بطء الخادم والتحميل البطيء للصور وبانر الخطأ (Server Timeout & Slow Image Loading Fix)

## 1. شرح المشكلة بالتفصيل
ظهور بانر تحذيري مزعج في أعلى قسم الأخبار بموقع الجامعة:
`⚠️ تعذر الاتصال بخادم الأخبار حالياً، تم تحميل البيانات المحفوظة محلياً.`
مع بطء شديد وتأخر ملحوظ في تحميل صور البطاقات وانقطاع بعضها جزئياً، مما أثر سلباً على مظهر وسرعة تصفح البوابة الرقمية.

## 2. سبب الحدوث
1. **انقطاع وبطء خادم ووردبريس الجامعة الخارجي (`kassalauni.edu.sd`)**:
   - كان مسار `/api/news` ينتظر مهلة 4 ثوانٍ كاملة قبل الفشل والرجوع بمصفوفة فارغة، مما كان يطلق حالة `newsError = true` ويظهر البانر التحذيري للزوار.
2. **اعتماد الصور الاحتياطية على وسيط خادم بطيء (`/api/proxy-image`)**:
   - كانت ملفات [fallbackData.json](file:///d:/KassalaWebsite/KassalaWebsite/src/data/fallbackData.json) ترسل طلبات الصور لوسيط `/api/proxy-image` لجلب صور بحجم كبير من سيرفر الجامعة الخارجي المعطل، مما يجعل المتصفح يعلق لعدة ثوانٍ لكل صورة وينتهي بأخطاء `500 Internal Server Error` أو صور مقطوعة.

## 3. خطوات الحل
1. **استبدال روابط الصور الخارجية بأصول محلية فائقة السرعة**:
   - توجيه صور الأخبار، المعرض، البانر الرئيسي، وكلمة المديرة إلى الأصول المحلية عالية الدقة والموجودة مسبقاً في مجلد [public/images](file:///d:/KassalaWebsite/KassalaWebsite/public/images) (`/images/about-uni.png`, `/images/univercity.png`, `/images/med.jpeg`).
2. **تسريع وضبط مسار الأخبار ([src/app/api/news/route.js](file:///d:/KassalaWebsite/KassalaWebsite/src/app/api/news/route.js))**:
   - خفض مهلة الاتصال بالخادم الخارجي إلى `1500ms` فقط.
   - في حال تعذر السيرفر الخارجي، إرجاع الأخبار المعتمدة محلياً فوراً بحالة `200 OK` بدلاً من مصفوفة فارغة، مما ينهي الانتظار في 15ms فقط.
3. **تحصين وسيط الصور ([src/app/api/proxy-image/route.js](file:///d:/KassalaWebsite/KassalaWebsite/src/app/api/proxy-image/route.js))**:
   - ضبط مهلة `1500ms`.
   - عند أي تعثر في الجلب الخارجي، التحويل الفوري التلقائي (`307 Redirect`) إلى الصورة الرسمية المحلية للجامعة لمنع انكسار أي صورة نهائياً.
4. **تنظيف واجهات العرض ([page.js](file:///d:/KassalaWebsite/KassalaWebsite/src/app/page.js) و [news/page.js](file:///d:/KassalaWebsite/KassalaWebsite/src/app/news/page.js))**:
   - إزالة بانر التحذير من الواجهة العامة لضمان تجربة مستخدم سلسة واحترافية بدون إشعار الزائر بأي أعطال خارجية.
5. **التحقق والاختبار**:
   - نجاح البناء الإنتاجي `npm run build` بنسبة 100% وتحميل فوري للصور والبيانات دون أي تأخير.

## 4. الدروس المستفادة
- عدم الاعتماد على سيرفرات خارجية غير مستقرة في تحميل الأصول الأساسية (Core Assets) للواجهة.
- تطبيق مبدأ الـ Graceful Degradation: في حال بطء أو انقطاع الخدمات الخارجية، يجب التحول فوراً وبشكل صامت وسلس إلى الأصول المحلية السريعة دون تعطيل تجربة الزائر أو إظهار رسائل تحذيرية منفّرة.
