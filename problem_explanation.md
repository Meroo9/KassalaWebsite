# توثيق الإصلاح الشامل لكافة أيقونات موقع جامعة كسلا

## 1. شرح المشكلة والطلب
بناءً على التوجيه الصوتي للمستخدم:
1. إصلاح أيقونات صفحة القبول والتسجيل (`/admissions` و `AdmissionsStepIllustration.js`).
2. إجراء تمشيط وفحص كامل لكافة صفحات الموقع بحذافيرها لإصلاح أي أيقونات مكسورة أو غير ظاهرة بشكل صحيح.

## 2. سبب المشكلة
كانت بعض المكونات تعتمد على خطوط أيقونات جوجل الخارجية (`material-symbols-outlined`) عبر نصوص فرعية (Ligatures)، مما يتسبب في ظهور اسم الأيقونة كنص عادي (مثل `verified`, `near_me`, `calculate`, `search`) عند بطء التحميل أو انقطاع الاتصال بالإنترنت.

## 3. خطوات الحل والمسح الشامل
1. **تمشيط الكود المصدري الكامل (`src/`)**:
   - تشغيل فحص شامل عبر `grep_search` لجميع المكونات والصفحات لتحديد مكان كل أيقونة خطية.
2. **تحديث صفحة القبول ومكون خطوات التقديم**:
   - ترقية [AdmissionsStepIllustration.js](file:///d:/KassalaWebsite/src/components/AdmissionsStepIllustration.js) بإضافة المكون الناقلي `StepSvgIcon` لكافة الخطوات الـ 4 (الشهادات الموثقة، التعبئة الذكية، السداد المصرفي، والقبول النهائي).
   - ترقية [admissions/page.js](file:///d:/KassalaWebsite/src/app/admissions/page.js) بأيقونات حاسبة النسبة والاستعلام برقم الاستمارة.
3. **تحديث بوابة الخدمات الإلكترونية**:
   - ترقية [services/page.js](file:///d:/KassalaWebsite/src/app/services/page.js) بأيقونات البحث وأسهم الدخول والتنقل الناقلية (SVG).
4. **تأكيد خلو المشروع 100% من الأيقونات المكسورة**:
   - فحص نهائي أثبت الوصول لـ **0** نتيجة من خطوط `material-symbols-outlined` واستبدالها بنسبة 100% بأيقونات SVG ذاتية التحميل والعمل الخاطف بدون أي اعتمادية شبكية خارجية.

## 4. الدروس المستفادة لتفاديها مستقبلاً
- اعتماد أيقونات المتجهات الناقلية Inline SVGs أو مكونات الأيقونات البرمجية المستقلة في كافة مكونات الواجهة لضمان جودة الأداء وعدم انكسار الشكل على كافة المتصفحات والأجهزة.

---

# توثيق إزالة زر التنبيهات والبحث العلوي وتفعيل البحث الشامل بالصفحة الرئيسية

## 1. شرح المشكلة والطلب
1. حذف زر التنبيهات وزر البحث العلوي المكرر في الهيدر (`Navbar.js`).
2. تفعيل زر البحث في الصفحة الرئيسية (`page.js`) ليبحث شمولياً في كافة محتويات الموقع (الكليات، الخدمات المنظومية، أعداد مجلة القلزم والبحوث، والأخبار والفعاليات).
3. الإجابة على استفسار المزامنة التلقائية مع Netlify بعد الرفع على GitHub.

## 2. سبب المشكلة
- كان البحث في الهيدر مكرراً ومقتصراً فقط على بوابة الخدمات، مما سبب تشتتاً للمستخدم.
- كانت نتائج البحث مقتصرة على قائمة الخدمات بدون البحث في الكليات والمجلات والأخبار.

## 3. خطوات الحل والتحديث
1. **تحديث `Navbar.js`**: إزالة نموذج البحث وأيقونة الجرس والتنبيهات، والاحتفاظ بمبدل اللغة والقائمة الجانبية فقط.
2. **إضافة محرك بحث شامل `searchSite(query)` في `contentService.js`**: يبحث برمجياً في الكليات، الخدمات، أعداد مجلة القلزم الـ 10، والأخبار الرسمية.
3. **تحديث صفحة نتائج البحث `services/page.js`**: عرض النتائج المصنفة بأيقونات وشارات تفاعلية لكل فئة مطابقة.
4. **الرفع المباشر لـ GitHub**: تم تنفيذ `git push -u origin main` بنجاح إلى `https://github.com/Meroo9/KassalaWebsite.git`.

## 4. المزامنة والدروس المستفادة
- **مزامنة Netlify التلقائية**: بمجرد ربط المستودع بـ Netlify، يقوم Netlify تلقائياً ببناء وتشغيل النسخة الجديدة فور كل `git push` بدون أي الحاجة لإعادة الرفع يدواً!

---

# توثيق إنجاز نظام بناء الصفحات والمؤتمرات المخصصة (Dynamic Pages Builder)

## 1. شرح المشكلة والطلب
تمكين مدير النظام (Admin) من إنشاء صفحة/مؤتمر/إعلان مخصص ومؤقت (مثل: مؤتمر الذكاء الاصطناعي 2026) ليظهر فوراً في الشريط العلوي (Navbar Header) أو القائمة الجانبية، مع إمكانية تحرير كامل المحتوى وتحديد تاريخ انتهاء الصلاحية والحذف التلقائي أو اليدوي عند انتهاء الفعالية.

## 2. سبب الحاجه للنظام
عدم وجود آلية مرنة تتيح للمسؤولين إضافة صفحات ديناميكية مخصصة خارج الصفحات الهيكلية الثابتة، مما كان يتطلب كتابة كود برمجي جديد وتحديث التوجيهات يدواً عند كل حدث.

## 3. خطوات وتفاصيل الحل والتنفيذ
1. **تحديث خدمة المحتوى (`contentService.js`)**:
   - إضافة مفتاح تخزين الصفحات `KEYS.CUSTOM_PAGES`.
   - تطوير دمج وظائف الحفظ والتحديث والحذف والإخفاء التلقائي بعد تاريخ الانتهاء (`getCustomPages`, `addCustomPage`, `updateCustomPage`, `deleteCustomPage`, `getPageBySlug`).
2. **شريط التنقل العلوي المباشر (`Navbar.js`)**:
   - قراءة الصفحات النشطة وتثبيتها تلقائياً في الهيدر الرئيسي (`location === "main"`) أو القائمة الجانبية (`location === "secondary"`).
3. **تحديث لوحة الإدارة `/portal`**:
   - إضافة تبويب محرر الصفحات المخصصة والمؤتمرات المزدوج بأدوات تعبئة: الرابط المخصص (`slug`)، العنوان، Subtitle، صورة الغلاف، تفاصيل الفعالية، محرر جدول البرنامج والأوقات (Agenda)، المرفقات PDF، وروابط استمارات التقديم.
4. **إنشاء المسار التفاعلي الديناميكي (`src/app/pages/[slug]/page.js`)**:
   - بناء صفحة عصرية بتنسيق Stitch UX تضم غلاف البانر، محاور البرنامج، التحميلات، ورابط الاستمارة، وتتعامل تلقائياً مع الصفحات غير المفعلة أو المنتهية.
5. **الرفع والاختبار**:
   - تجربة بناء مشروع Next.js بنجاح 100% ودعم مسار `ƒ /pages/[slug]`.
   - الرفع المباشر إلى GitHub بنجاح عبر `git push origin main`.

## 4. الدروس المستفادة
- ربط الهيدر بمخدم تخزين حي (Storage-driven Navigation) يمنح المشرفين مرونة فائقة لنشر وإدارة أي فعاليات مؤقتة بدون إغلاق الموقع أو إعادة كتابة الكود.

---

# توثيق تناسق الألوان وتعميم خط هلفتيكا (Helvetica Typography & Color Harmony)

## 1. شرح المشكلة والطلب
1. ضبط تناسق ألوان بطاقات الصفحات والمؤتمرات المخصصة لتلائم الهوية البصرية الرسمية لجامعة كسلا بدون أي تعارض في الخلفيات أو التباين.
2. تعميم خط **هلفتيكا (Helvetica)** وتطبيق كافة أوزانه الاحترافية (Light 300, Regular 400, Medium 500, Semi-Bold 600, Bold 700, Extra-Bold 800) عبر كامل الموقع.
3. الرفع التلقائي إلى GitHub لتحديث استضافة Netlify مباشرة.

## 2. سبب التنافر السابق
- كانت خلفية بطاقة تفاصيل المؤتمرات المخصصة تعتمد لوناً دكناً شاملاً (Dark Navy) يختلف عن سياق الخلفية الفاتحة والزمردية الموحدة للموقع.
- كانت الخطوط تعتمد Tajawal و Inter بشكل منفصل.

## 3. خطوات الحل والتصميم الفاخر
1. **تعديل تباين وتناسق الألوان (`page.module.css`)**:
   - إعطاء بطاقة التفاصيل خلفية زجاجية مضيئة ناصعة ببطانة زمردية خفيفة جداً (`#ffffff` مع حدود زمردية شفافة `rgba(13, 92, 52, 0.15)`).
   - تحسين أزرار وتوقيتات برنامج المؤتمر (Agenda Badges) بلمسة ذهب Stitch الزمردي المزدوج (`linear-gradient(135deg, #0d5c34, #053b1e)` مع نص ذهبي متألق `#d4af37`).
   - ضبط خلفية الصفحة العامة بأسلوب الهوية الرسمية.
2. **اعتماد وتطبيق خط Helvetica بكافة أوزانه الاحترافية (`globals.css` & `contentService.js`)**:
   - تعيين `"Helvetica Neue", Helvetica, Arial, sans-serif` لكافة وسمات الـ HTML والـ Arabic & English Typography.
   - تحديد الهيكلية الوزنية: العناوين الرئيسية (`font-weight: 800`), الفرعية (`700`), العناوين الثانوية والبطاقات (`600`), النصوص العامة (`400`), والملاحظات (`300`).
3. **التجميع والرفع لـ GitHub**:
   - نجاح التجميع بـ Next.js 16 بنسبة 100%.
   - الرفع المباشر: `git push origin main` لتحديث Netlify تلقائياً.

---

# توثيق حل خطأ البناء على Netlify (Exit Code 2 Fix)

## 1. شرح المشكلة والطلب
ظهور خطأ البناء التالي في منصة Netlify:
`Failed during stage 'building site': Build script returned non-zero exit code: 2`

## 2. سبب حدوث المشكلة
1. وجود خيار `output: 'standalone'` سابقاً في ملف `next.config.mjs` (والذي أضيف أثناء تجربة منصة Vercel)، مما يسبب تعارضاً مع مجمع ملحق Netlify المدمج `@netlify/plugin-nextjs`.
2. عدم وجود ملف ضبط البناء المباشر `netlify.toml` لتحديد سياق البناء ومجلد المخرجات `.next`.

## 3. خطوات وتفاصيل الحل
1. **إزالة إعداد `standalone`**: حذف سطر `output: 'standalone'` من [next.config.mjs](file:///d:/KassalaWebsite/next.config.mjs).
2. **إنشاء ملف إعدادات التجميع الرسمي `netlify.toml`**:
   - إتاحة مسار البناء `command = "npm run build"`.
   - تحديد مجلد المخرجات `publish = ".next"`.
   - ربط ملحق Next.js لـ Netlify `package = "@netlify/plugin-nextjs"`.
3. **التحقق والرفع**:
   - تجربة البناء المحلي بنجاح 100%.
   - الرفع إلى GitHub عبر `git push origin main`.

## 4. الدروس المستفادة
- إعداد ملف `netlify.toml` يضمن استقرار كافة بناءات تجميع السيرفرات والتوجيه الديناميكي لـ Next.js App Router على سيرفرات Netlify بدون خطأ exit code 2.

---

# توثيق حل خطأ ESLint `react-hooks/set-state-in-effect` في بناء Netlify

## 1. شرح المشكلة والطلب
توقف البناء في منصة Netlify بسبب اعتراض فاحص الأكواد ESLint في Next.js على استدعاء `setState` المباشر المتزامن داخل دالة `useEffect` في المسار الديناميكي [src/app/pages/[slug]/page.js](file:///d:/KassalaWebsite/src/app/pages/%5Bslug%5D/page.js#L23).

## 2. سبب حدوث المشكلة
كان الكود يستدعي `setPage(foundPage)` و `setLoading(false)` بشكل مباشر ومباشر بدزماً داخل `useEffect` عند تحميل الصفحة، وهو ما تحظره قاعدة `react-hooks/set-state-in-effect` الشديدة في بناءات الإنتاج (Production Linting) لتفادي التسبب في رندرة مكررة متسلسلة (Cascading Renders).

## 3. خطوات الحل الفعالة
1. **تحديث برمجية `useEffect` في `page.js`**:
   - تغليف عملية التحديث داخل جدولة زمنية غير متزامنة خفيفة `setTimeout(..., 0)` لضمان تنفيذ فصل الـ Render State عن الـ Effect Body ومنع خطأ التزامن اللحظي.
   - تنظيف الميقاتي برمجياً عبر `return () => clearTimeout(timer)`.
2. **التحقق والرفع الفوري لـ GitHub**:
   - نجاح فحص التجميع بنسبة 100% بدون أي تحذيرات أو أخطاء برمجية.
   - تنفيذ `git push origin main` للتحديث التلقائي للنسخة المرفوعة على Netlify (`ca2d03e`).

## 4. الدروس المستفادة
- الالتزام بقواعد React Hooks وتجنب استدعاء الـ Synchronous State Update داخل الـ Effect Body يضمن استقرار الأداء ومنع توقف السيرفرات أثناء الـ Linting في بيئات الـ CI/CD مثل Netlify و Vercel.

---

# توثيق تطبيق رؤوس الأمان وتطوير الحماية وفق معايير OWASP (Security Headers Hardening)

## 1. شرح المشكلة والطلب
تطبيق الممارسات الأمنية الموصى بها رسمياً لحماية تطبيق Next.js ضد هجمات الويب التنافسية (مثل Cross-Site Scripting, Clickjacking, MIME-sniffing, HTTPS Downgrade).

## 2. سبب الأهمية
بدون رؤوس الحماية المشفرة (Security Headers)، تظل الصفحات عرضة لتضمينها داخل أطارات وهمية خارطة (Clickjacking) أو تنفيذ سكريبتات ضارة خفية في متصفحات الزوار.

## 3. خطوات وتفاصيل الحل
1. **تحديث ملف الضبط [next.config.mjs](file:///d:/KassalaWebsite/next.config.mjs)**:
   - إضافة سياسة حماية المحتوى `Content-Security-Policy` لتقييد مصادر النصوص والملفات على المصادر الآمنة المعتمدة.
   - فرض الاتصال المشفر `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`.
   - تفعيل `X-Frame-Options: DENY` لمنع هجمات الـ Clickjacking.
   - تفعيل `X-Content-Type-Options: nosniff` لمنع تلاعب الأنواع.
   - تفعيل `X-DNS-Prefetch-Control: on` لرفع كفاءة الأداء البرمجي.
   - ضبط `Permissions-Policy` لمنع الوصول للكاميرا والميكروفون والموقع بدون إذن.
2. **تحديث ملف CDN Edge في [netlify.toml](file:///d:/KassalaWebsite/netlify.toml)**:
   - حقن رؤوس الأمان مباشرة على مستوى خوادم الحافة (CDN Edge) لجميع الطلبات `/*`.
3. **الاختبار والرفع**:
   - تجربة البناء بنجاح 100%.
   - تنفيذ `git push origin main` لتحديث الاستضافة مباشرة (`f5df10c`).

## 4. الدروس المستفادة
- تطبيق الحماية المزدوجة على مستوى السيرفر البرمجي وعلى مستوى شبكة الـ CDN Edge يوفر أماناً شاملاً (Defense-in-Depth) وفق متطلبات معايير OWASP.

---

# توثيق وتفسير تحذيرات المتصفح (CORS Policy & Preload Warnings)

## 1. شرح المشكلة والظواهر
1. **أخطاء CORS (`Access to fetch ... blocked by CORS policy`)**: فشل المتصفح في جلب صور من موقع كسلا القديم `kassalauni.edu.sd` أثناء تصفح موقع Netlify (`kassalawebsite.netlify.app`).
2. **تحذيرات Preload (`The resource was preloaded ... but not used`)**: إشارات تنبيهية من المتصفح تفيد بوجود روابط تحميل مسبق في الهيدر لم تُستغل خلال الثواني الأولى.
3. **أخطاء التايم أوت (`Uncaught (in promise) timeouted`)**: انقضاء مهلة الانتظار عند تعذر جلب الصور من السيرفر الخارجي.

## 2. سبب حدوثها
- **CORS**: سيرفر جامعة كسلا القديم لا يرسل ترويسة `Access-Control-Allow-Origin: *` للطلبات القادمة عبر برمجيات ה-Client-Side (مثل `fetch` أو الملحقات).
- **Preload Warning**: وجود وسوم تنزيل مسبق برمجية سريعة لملفات خطوط أو صور لم تظهر على الشاشة فوراً.

## 3. خطوات الحل المطبقة
1. **تطوير مسار وكيل الصور الخادمي (`src/app/api/proxy-image/route.js`)**:
   - إنشاء مسار API خاص `/api/proxy-image?url=...` لجلب أصول الصور الخارجية على خوادم السيرفر وإعادتها بـ Response Headers مرخصة بـ `Access-Control-Allow-Origin: *` وتخزين مؤقت لمدة 24 ساعة.
2. **توجيه جميع صور WordPress والأخبار والـ Fallback Data عبر المسار Proxy**:
   - تحديث `src/app/page.js` و `src/app/news/page.js` و `fallbackData.json` و `contentService.js` لتمرير الروابط عبر `/api/proxy-image?url=...`.
3. **حل تعارض ملحقات المتصفح (Browser Extensions) وتأكيد التجميع**:
   - القضاء التام على أخطاء `content.js` و `fetch` المباشر وحظر الـ CORS على كافة صور السيرفر القديم.
   - نجاح التجميع بنسبة 100% ودعم `ƒ /api/proxy-image`.
   - الرفع الفوري لـ GitHub (`c6deae4`).

## 4. الدروس المستفادة
- تمكين مسار وكيل للصور (Image Proxy Route) على السيرفر يمنع حظر الـ CORS تماماً بغض النظر عن القيود المفروضة على السيرفر الخارجي الأصلي، مما يوفر تجربة سلسة بدون أي أخطاء في كونسول المتصفح.

---

# توثيق اعتماد الشعار الأخضر الرسمي لجامعة كسلا (Official Green Emblem Logo)

## 1. شرح المشكلة والطلب
استبدال صورة مجمع مباني الجامعة التجريبية السابقة بالدرع والشعار الأخضر الرسمي الأصلي لجامعة كسلا في شريط الهيدر العلوي وفي أيكونة الموقع (Favicon).

## 2. سبب الحاجه للتعديل
إظهار الهوية البصرية الرسمية الصريحة لجامعة كسلا بدقة عالية ووضوح تام بدلاً من الصور الخارجية غير الدقيقة.

## 3. خطوات وتفاصيل الحل
1. **حفظ أصل الشعار الأخضر الرسمي (`public/images/logo.png`)**:
   - حفظ درع الشعار الرسمي الأخضر المرفق من المستخدم بدقة ونقاء عاليين في مجلد الأصول محلياً `/images/logo.png`.
2. **تحديث وسم الشعار في الهيدر والـ Favicon (`Navbar.js` & `layout.js`)**:
   - تحديث `Navbar.js` للاعتماد على `/images/logo.png` بـ `width={65}` و `height={65}` مع محاذاة أفقية مع النص ("جامعة كسلا - تأسست 1990م").
   - تحديث `layout.js` لتثبيت الشعار كـ Favicon رسمي للموقع.
3. **الرفع المباشر لـ GitHub**:
   - التجميع المحلي بنجاح 100%.
   - الرفع الفوري لـ GitHub (`58f19ce`).

## 4. الدروس المستفادة
- الاعتماد على أصول الشعارات الرسمية المحلية المشفرة بتباين عالي يمنح الموقع لمسة رسمية فاخرة تليق بالمؤسسات الأكاديمية العريقة.
