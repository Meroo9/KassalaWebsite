# توثيق تضمين خريطة تفاعلية مصغرة في الفوتر (Footer Mini Map Embed)

## 1. شرح المشكلة بالتفصيل
الحاجة إلى إظهار خريطة موقع جامعة كسلا (مجمع كليات الطب والعلوم الصحية) بشكل مصغر وأنيق في الفوتر (أسفل نبذة الجامعة ومواقع التواصل) لتسهيل وصول الزوار إلى الموقع الجغرافي مباشرة من أي صفحة.

## 2. سبب الحدوث
طلب إضافة وتضمين إطار Google Maps التفاعلي ضمن مكون الفوتر العام بطريقة متجاوبة ومنسقة.

## 3. خطوات الحل
1. **تضمين الـ iframe في الفوتر**:
   - إدراج رابط خريطة Google Maps التفاعلي داخل حاوية `miniMapContainer` في العمود الأول بـ [Footer.js](file:///d:/KassalaWebsite/src/components/Footer.js).
   - ضبط `loading="lazy"` و `referrerPolicy="strict-origin-when-cross-origin"`.
2. **تنسيق الخريطة المصغرة (CSS)**:
   - إنشاء حاوية بحدود ناعمة (`border-radius: 12px`) وإطار متناسق وظل أنيق في [Footer.module.css](file:///d:/KassalaWebsite/src/components/Footer.module.css).
   - إضافة تأثير حركي خفيف عند التمرير (`hover`).
3. **التحقق**:
   - نجاح البناء `npm run build` بنسبة 100%.

## 4. الدروس المستفادة
- تضمين الخرائط التفاعلية كمعاينات مصغرة في الفوتر يعزز تجربة المستخدم ويسهل الاستدلال الجغرافي دون التأثير على سرعة تحميل الصفحة.
